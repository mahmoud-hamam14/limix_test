import 'package:flutter/material.dart';

class SensorPerformanceWidget extends StatelessWidget {
  const SensorPerformanceWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}